import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function IntegrationsPage() {
  return (
    <PlaceholderPage
      title="Integrations"
      description="This tab is intentionally UI-only for now. We can connect provider accounts and sync status in a later pass."
    />
  );
}
