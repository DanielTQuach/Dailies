import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function ResourcesPage() {
  return (
    <PlaceholderPage
      title="Resources"
      description="This tab is ready for a resources library once we decide categories and saved-item behavior."
    />
  );
}
